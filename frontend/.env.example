# ── frontend environment ──
# copy this file to .env and adjust if needed.

# backend base url. defaults to http://localhost:3001 when unset.
# only needed for tools that call the backend (summarize, translate, merge, etc.).
# the annotate / signature tools do not use this.
VITE_API_URL=http://localhost:3001
